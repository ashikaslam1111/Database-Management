use hr;
select   distinct region_id  from countries; /*  to find the distinct elent from the table */

/* use of order by there are two kind of ordering asending and disanding */
select * 
from departments
order by location_id  asc;
